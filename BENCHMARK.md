<!-- Generated by `swift run choir-benchmark`. Do not edit by hand. -->

> **Baseline, not a shipping measurement.** The acoustic model is a mock and the
> vocoder is Griffin-Lim, so throughput figures describe the current pipeline
> rather than a trained voice. That is the point: `PRF-040` exists to produce a
> baseline, and one taken *before* the model lands is what makes the model's
> cost measurable when it arrives.
>
> **Cold start is a single sample by nature** — a true cold start includes
> process launch and the first lexicon load, neither of which recurs in-process.
> Across repeated runs on this host it ranged 0.62–2.99 s against a 1.50 s
> target, so it straddles the requirement rather than clearing it. See the
> lexicon-load row for the dominant component.
>
> `PRF-040` requires this report on **every** reference device per release. This
> run covers R3 only:
>
> ```
> swift run choir-benchmark --device R1 --output BENCHMARK-R1.md
> ```

# CHOIR Benchmark Report

- **Package version:** 0.12.0
- **Host:** macOS on arm64
- **Reference device:** R3 — MacBook (M2 or later)

| Requirement | Measurement | Median | Range | Samples | Target | Status |
|---|---|---:|---:|---:|---:|---|
| `PRF-011` | Cold start to first audio | 0.89 s | — | 1 | 1.50 s | PASS |
| `PRF-001` | Batch RTF | 0.04 RTF | 0.04–0.08 | 9 | 0.15 RTF | PASS |
| `PRF-010` | Warm TTFA | 108.75 ms | 89.74–259.85 | 9 | 250.00 ms | PASS |
| `PRF-011` | Lexicon load | 0.97 s | 0.82–1.17 | 9 | — | no target |
| `PRF-020` | Peak resident memory | 33.50 MB | — | 1 | — | no target |
| `PRF-030` | Installed asset size | 3.45 MB | — | 1 | 400.00 MB | PASS |

## Caveats

- **PRF-011** Cold start to first audio: Single sample by nature: a true cold start includes process launch and the first lexicon load, neither of which recurs in-process. Repeat the whole command for a distribution. Measured against the mock acoustic model and Griffin-Lim vocoder; not representative of a trained model.
- **PRF-001** Batch RTF: Measured against the mock acoustic model and Griffin-Lim vocoder; not representative of a trained model.
- **PRF-010** Warm TTFA: Measured against the mock acoustic model and Griffin-Lim vocoder; not representative of a trained model.
- **PRF-011** Lexicon load: Component of cold start, not a requirement in itself. Sampled with a fresh lexicon each time; the shared instance loads once per process.
- **PRF-020** Peak resident memory: Whole-process RSS, which includes the test host; the requirement asks for memory attributable to CHOIR alone.
- **PRF-030** Installed asset size: Voice model assets do not exist yet; this is the lexicon and licence files only.

## Not measured by this harness

- PRF-021 battery: requires 60 minutes of continuous streaming on a physical R1 with playback; not measurable from a test process.
- PRF-010 sustained streaming RTF: requires a long-running stream on the target device.
- PRF-001/010/011 on R1, R2, R4 and R5: this harness measures the device it runs on. Per PRF-040 it must be run on each reference device and the reports collected.
